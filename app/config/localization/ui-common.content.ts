import { t, type Dictionary } from "intlayer";

const uiCommonContent = {
  key: "ui-common",
  content: {
    close: t({ en: "Close", ko: "닫기" }),
  },
} satisfies Dictionary;

export default uiCommonContent;

